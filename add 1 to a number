#include<stdio.h>
int input()
{
	int x;
	printf("enter a number");
	scanf("%d",&x);
	return x;
}
int compute(int a)
{
	a=a+1;
	return a;
}
void output(int b)
{
	printf("The new number is %d",b);
	
}
int main()
{
	int x,y;
	x=input();
	y=compute(x);
	output(y);
        return 0;
}
